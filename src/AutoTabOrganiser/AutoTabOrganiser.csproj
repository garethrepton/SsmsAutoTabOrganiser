<Project>

  <Import Project="Sdk.props" Sdk="Microsoft.NET.Sdk" />

  <PropertyGroup>
    <TargetFramework>net472</TargetFramework>
    <LangVersion>latest</LangVersion>
    <RootNamespace>AutoTabOrganiser</RootNamespace>
    <AssemblyName>AutoTabOrganiser</AssemblyName>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <IsPackable>false</IsPackable>
    <UseWPF>true</UseWPF>

    <GeneratePkgDefFile>true</GeneratePkgDefFile>
    <UseCodebase>true</UseCodebase>
    <CreateVsixContainer>true</CreateVsixContainer>
    <!-- DeployExtension auto-installs into a VS Experimental hive on F5; off here so headless
         Build-Tools-only machines just produce the .vsix (no IDE present to deploy into). -->
    <DeployExtension>false</DeployExtension>
    <IncludeAssemblyInVSIXContainer>true</IncludeAssemblyInVSIXContainer>
    <IncludeDebugSymbolsInVSIXContainer>true</IncludeDebugSymbolsInVSIXContainer>
    <IncludeDebugSymbolsInLocalVSIXDeployment>true</IncludeDebugSymbolsInLocalVSIXDeployment>

    <StartAction>Program</StartAction>
    <StartProgram>$(DevEnvDir)devenv.exe</StartProgram>
    <StartArguments>/rootsuffix Exp</StartArguments>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.VSSDK.BuildTools" Version="17.10.2178">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.VisualStudio.SDK" Version="17.0.32112.339" ExcludeAssets="runtime" />
    <PackageReference Include="Microsoft.VisualStudio.Imaging" Version="17.0.32112.339" ExcludeAssets="runtime" />
    <PackageReference Include="Microsoft.VisualStudio.ImageCatalog" Version="17.0.32112.339" ExcludeAssets="runtime" />
    <PackageReference Include="Community.VisualStudio.Toolkit.17" Version="17.0.522" />

    <PackageReference Include="Microsoft.Data.Sqlite.Core" Version="6.0.36" />
    <!-- HARD CEILING for the managed refs: SSMS 22 supplies SQLitePCLRaw 3.0.2.2801 at
         runtime (PublicAssemblies\SQLite) and the VSIX has never shipped the managed
         assemblies. Binding only works DOWNWARD: refs at/below 3.0.2 redirect up to
         SSMS's copy; a ref above it (3.0.3, shipped as v0.1.74) dies at startup with
         'Could not load SQLitePCLRaw.batteries_v2'. 2.1.11 is proven in production.
         KNOWN TRADE-OFF: 2.1.11's native is SQLite 3.49.1, which carries CVE-2025-6965
         (fixed in 3.50.x). Accepted for now to keep the dependency graph simple — the
         3.50.4 alternative needs the SourceGear.sqlite3 package and its AnyCPU
         workarounds; see this file's git history (July 2026) if revisiting. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="2.1.11" />
    <PackageReference Include="Markdig" Version="1.1.3" />
    <PackageReference Include="System.Text.Json" Version="6.0.10" />
    <!-- Compile-graph-only pin: the VS SDK's transitive MessagePack 2.2.85 carries
         CVE-2024-48924 (and 2.5.187 carries CVE-2026-48109/48506). Nothing here ships at
         runtime (SSMS supplies its own copies); this lifts the resolved version so
         vulnerability scans run clean. -->
    <PackageReference Include="MessagePack" Version="2.5.301" ExcludeAssets="runtime" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="System.Design" />
    <Reference Include="System.ComponentModel.Composition" />
    <Reference Include="EnvDTE">
      <EmbedInteropTypes>false</EmbedInteropTypes>
    </Reference>
    <Reference Include="PresentationCore" />
    <Reference Include="PresentationFramework" />
    <Reference Include="System.Xaml" />
    <Reference Include="WindowsBase" />
    <!-- For System.Windows.Forms.ColorDialog used by the tag colour configuration UI. -->
    <Reference Include="System.Windows.Forms" />
    <Reference Include="System.Drawing" />
    <!-- For the Export-archive command (ZipFile / ZipArchive). -->
    <Reference Include="System.IO.Compression" />
    <Reference Include="System.IO.Compression.FileSystem" />
  </ItemGroup>

  <ItemGroup>
    <None Include="source.extension.vsixmanifest">
      <SubType>Designer</SubType>
    </None>
    <Content Include="LICENSE.txt">
      <IncludeInVSIX>true</IncludeInVSIX>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Storage\Schema.sql">
      <IncludeInVSIX>true</IncludeInVSIX>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <!-- In the 2.x line the native e_sqlite3.dll ships in sqlitepclraw.lib.e_sqlite3,
       versioned in lockstep with the bundle reference above — the version here must
       match it. The Error keeps drift loud: a silent Exists() skip here once produced
       a VSIX with no native at all on a clean machine. -->
  <PropertyGroup>
    <SqliteNativePath>$(NuGetPackageRoot)sqlitepclraw.lib.e_sqlite3\2.1.11\runtimes\win-x64\native\e_sqlite3.dll</SqliteNativePath>
  </PropertyGroup>

  <Target Name="IncludeSqliteNativeInVsix" AfterTargets="ResolveAssemblyReferences" BeforeTargets="GetCopyToOutputDirectoryItems;BuiltProjectOutputGroup">
    <Error Condition="!Exists('$(SqliteNativePath)')"
           Text="Native SQLite not found at '$(SqliteNativePath)'. The version in SqliteNativePath has drifted from the SQLitePCLRaw.bundle_e_sqlite3 reference — keep them identical, or the VSIX would ship without e_sqlite3.dll." />
    <ItemGroup>
      <Content Include="$(SqliteNativePath)">
        <Link>e_sqlite3.dll</Link>
        <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
        <IncludeInVSIX>true</IncludeInVSIX>
      </Content>
    </ItemGroup>
  </Target>

  <ItemGroup>
    <VSCTCompile Include="VSCommandTable.vsct">
      <ResourceName>Menus.ctmenu</ResourceName>
    </VSCTCompile>
  </ItemGroup>

  <Import Project="Sdk.targets" Sdk="Microsoft.NET.Sdk" />

  <Import Project="$(VsSDKInstall)\Microsoft.VsSDK.targets" Condition="'$(VsSDKInstall)' != ''" />

</Project>
